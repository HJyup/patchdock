{
  "name": "patchdock-agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent definitions for this repository. `npm install` puts @patchdock/sdk in node_modules, which serves two purposes: editors resolve the SDK while authoring, and the Dockerfile copies that same tree into the agent image.",
  "dependencies": {
    "@patchdock/sdk": "alpha"
  },
  "devDependencies": {
    "@types/node": "^25.9.3"
  }
}
