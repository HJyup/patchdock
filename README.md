# patchdock

# Architecture

cli package encapsulates conenction between cobra and uses app pakcage to actually exectutre commands.
Important: CLI pakcage should not know about anything except app in genereal
