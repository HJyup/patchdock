{
  "name": "patchdock-agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent definitions for this repository. `pnpm install` links @patchdock/sdk into node_modules, which serves two purposes: editors and tsc resolve the SDK while authoring, and the Dockerfile copies that same tree into the image instead of vendoring SDK source.",
  "dependencies": {
    "@patchdock/sdk": "file:../sdk"
  },
  "devDependencies": {
    "@types/node": "^25.9.3"
  }
}
